# Changelog

All notable changes to this specification/documentation set are recorded here. This is a docs changelog (PRD/ERD/API spec/etc.). As of 2026-07-29 the application itself exists (see entries below) — build progress is tracked here too, phase by phase per [11-Development-Checklist.md](11-Development-Checklist.md), rather than in a separate release changelog.

## [Unreleased] - Application build progress

### Real landing page (2026-07-30)
- Found via the user's own live testing: the public homepage (`/`) had been serving the unmodified Laravel/React starter-kit's default "Welcome" boilerplate ("Let's get started", links to Laravel's own docs/Laracasts) through all 13 prior phases — every phase's work happened behind login, so nothing had touched the actual public-facing marketing page since Phase 0 scaffolding, and no wireframe/PRD doc ever specified one either.
- Replaced with a real AkuSehat landing page: hero + CTA, a 6-item feature grid, a 4-step "how it works" section, a pricing section rendering the real, live `plans` catalog (fetched server-side, so it always reflects whatever Admin has configured), and a closing CTA. Uses the same teal brand color already established by the PWA icon/theme-color.
- `GET /` now passes `plans` (active plans only) as an Inertia prop instead of rendering the page with no data.

### Phase 13 — PWA / Accessibility / Non-Functional Hardening (2026-07-30)
- **Production infrastructure gap found and fixed**: no persistent queue worker or scheduler was running for akusehat.web.id at all — every queued job and every `routes/console.php` scheduled job had only ever executed via manual `artisan queue:work`/tinker invocations during this project's live smoke tests, never automatically. Fixed by adding `/etc/supervisor/conf.d/akusehat-worker.conf` (2 `queue:work` processes + 1 `schedule:work` process), matching the exact pattern other Laravel sites on this shared box already use via Supervisor.
- PWA shell: `public/manifest.json`, `public/sw.js` (caches only content-hashed `/build/` assets + an `offline.html` fallback — deliberately never caches API/page data, since this app handles sensitive health data), a purpose-built `public/icon.svg`.
- Rate limiting added on every auth/AI-cost-triggering endpoint that didn't already have it: registration, forgot-password, AI provider connection test, AI-assistant chat messages, program start/regenerate.
- Data export job (PRD §13 right-to-export): `UserDataExportService` + `ExportUserDataJob`, delivered via a 24h signed download link. Caught a real bug via live smoke testing that the PHPUnit suite couldn't: a typo'd column name that SQLite silently tolerates (returns empty instead of erroring) but MySQL correctly rejects — see the new SQLite-lenient-column gotcha in project memory, the most dangerous variant of this pattern found so far since it produces *no error at all* under SQLite, not even a different one.
- Account deletion: verified `SoftDeletingScope` correctly excludes deleted users everywhere app-wide; fixed a real compliance-communication bug (self-service delete dialog claimed permanent deletion, which was never true); added Sanctum token revocation on deletion.
- Security pass (OWASP Top 10) and WCAG 2.1 AA + responsive QA passes conducted via systematic code-level audits (no browser-automation tooling available). Found and fixed 4 real accessibility issues (most notably: the Dashboard's daily checklist checkbox — the app's actual check-in mechanism — had no accessible label at all) and 2 systemic responsive-layout issues (5 data tables that clipped instead of scrolled on mobile; 7 form grids that didn't stack). Full writeups: [13-Security-Audit.md](13-Security-Audit.md), [14-Accessibility-Audit.md](14-Accessibility-Audit.md).
- Load test: with the newly-running persistent worker, 30- and 100-job concurrent `GenerateProgramJob` batches both drained in under ~1 second with 0 failures. Full methodology and the infrastructure-gap discovery: [15-Load-Test-Results.md](15-Load-Test-Results.md).
- Backup/restore runbook written ([16-Backup-Restore-Runbook.md](16-Backup-Restore-Runbook.md)); also found no automated backup exists anywhere on this server for any site — documented manual procedure + a proposed (not yet enabled) automation script, since enabling it requires an offsite-storage decision outside this session's authority to make unilaterally.
- 379/379 tests passing (34 new for Phase 13).

### Phase 12 — App Settings & Cross-Cutting (2026-07-30)
- `app_settings` migration (generic key-value store, matching `mysql.sql` exactly — `key`/`value`/`description`/`updated_at` only, no `created_at`) + `AppSettingsService` as the typed access layer over it, so callers never touch raw keys/JSON shapes directly.
- Resolved PRD §6.3's open business question ("Member brings their own AI key OR the platform provides a default shared provider/key") at the config level: `AppSettingsService::platformDefaultAiSetting()` builds an unsaved `UserAiSetting`-shaped object from the stored platform config (reusing that model's own encryption/relation handling rather than duplicating it), and `AIGatewayService::defaultSettings()` now falls back to it — but only when a user has configured *zero* providers of their own, never overriding a user's personal choice. Every call still logs under the real user's `user_id` in `ai_request_logs`, keeping per-user usage against a shared metered key measurable.
- `EnsureNotInMaintenanceMode` middleware, appended globally to the `web` group (DB-backed, Admin-toggleable — distinct from Laravel's file-based `php artisan down`, which needs shell access). Admins and `/login`/`/logout` always stay reachable. Added a custom `resources/views/errors/503.blade.php` for a clean, dependency-free maintenance page.
- `/admin/settings`: AI Provider Default + Maintenance Mode cards, gated by a new `app_settings.manage` permission, wired into `ActivityLogger`.
- Caught and fixed a brief self-inflicted production issue during smoke testing: this server *is* the live site, so appending new middleware to the global `web` group took effect on real traffic the moment the file was saved — and since the `app_settings` migration hadn't been run against the real database yet, every request site-wide 500'd for roughly a minute until the routine pre-smoke-test health check caught it. Fixed immediately by running the migration; no evidence of real visitor impact (only the health check itself hit the broken window). Documented as a generalizable lesson: any future phase touching global middleware needs its migration run the moment the code lands, not deferred to the end-of-phase smoke test like every prior phase's route-scoped changes could safely be.
- Maintenance mode itself was live-tested directly against production in the smallest possible window (enable → verify 503/bypass behavior → disable → re-verify 200), then fully cleaned up.
- 364/364 tests passing (20 new for Phase 12).

### Phase 11 — Subscription Scaffolding (2026-07-30)
- Migrations for `plans`, `subscriptions`, `payments` (schema + gating only per PRD §6.3 — no live payment gateway in v1). `PlanSeeder` seeds 3 designed tiers (Gratis/Premium Bulanan/Premium Tahunan) since neither the PRD nor wireframe name actual pricing.
- `SubscriptionService`: every user always resolves to a subscription — new registrations get one eagerly via a `Registered` event listener (`AssignDefaultPlan`), and `currentSubscription()` lazily backfills Gratis for anyone reached another way (pre-Phase-11 accounts, or right after a subscription expires with nothing to auto-renew it). `subscribe()` simulates an instantly-successful payment (`midtrans` used as the enum placeholder for "sandboxed", with an obviously-fake `provider_reference`, since v1 has no live gateway to actually redirect to). `cancel()` is cancel-at-period-end per the API spec — access continues until `ends_at`.
- `SubscriptionRenewalCheckJob` (daily): transitions subscriptions past `ends_at` to `expired` and notifies (`SubscriptionExpired`), flags ones expiring in exactly 7 days (`SubscriptionExpiringSoon`) — both mail+database per Phase 10's "one-time/significant event" precedent. No actual renewal is attempted since there's no live gateway; expired users simply fall back onto Gratis the next time their subscription is looked up.
- Plan-based gating: `max_programs` is real route middleware (`plan.program_limit`) on `POST /user-programs`, since it only needs the request's own user. `has_coach_access` is checked inline in `Admin\CoachAssignmentController::store()` instead, since that check needs the *target member* (a route param) rather than the acting admin — documented as a deliberate deviation from "middleware" in the literal sense, matching this app's existing pattern of trait/service-based checks for anything parameterized by a specific resource.
- API per 05-API-Specification.md §14: `GET /plans` (public catalog), `GET /subscription` (Inertia page — matching how every other Settings-tab endpoint in this app already works, not a raw JSON contract), `POST /subscription/subscribe`, `POST /subscription/cancel`, `GET /subscription/payments`. Admin: `GET/POST /admin/plans`, `PATCH /admin/plans/{id}`, `GET /admin/subscriptions` (filterable), gated by a new `subscriptions.manage` permission and wired into `ActivityLogger` per the Phase 9 admin-audit pattern.
- React: "Langganan" tab added to Settings (`/subscription`) — current plan/status/usage vs `max_programs`, a 3-plan comparison grid with upgrade buttons, a cancel-subscription dialog, and payment history. Admin `/admin/plans` (CRUD dialogs) and `/admin/subscriptions` (filterable list) added to the Admin sidebar section.
- A pre-existing Phase 8 test needed a genuine update, not a regression fix: `CoachAssignmentControllerTest`'s plain factory member now needs a paid subscription before an Admin can assign them a coach, since `has_coach_access` gating didn't exist when that test was written — a real cross-phase interaction, caught immediately by the full suite.
- 344/344 tests passing (27 new for Phase 11). Live HTTP smoke test against the real vhost: subscribed a real member to Premium Bulanan (verified the sandboxed `payments` row), confirmed a member on a plan without coach access gets a real 422 from Admin's coach-assignment endpoint while a Premium member succeeds, confirmed a free-plan member gets a real 403 trying to start a second program, ran `SubscriptionRenewalCheckJob` for real and drained the Redis queue to confirm the `SubscriptionExpired` notification completed without error (real SMTP), and confirmed `plan.updated`/`coach.assigned` land in the real `activity_logs` table.

### Phase 10 — Achievements & Notifications (2026-07-30)
- `achievements`/`user_achievements` migrations + models. `AchievementCriteriaEvaluator` designs 3 criteria types on top of the Database Dictionary's two loose example shapes (`weight_loss_kg`, `checklist_streak_days`, `program_milestone_days`), covering both "streaks" and "milestones" per FR-ACH-01. 6 baseline achievements seeded.
- `EvaluateAchievementsJob` (daily scheduled) checks every onboarded user against every achievement they haven't earned yet; the `uniq(user_id, achievement_id)` constraint makes it naturally idempotent.
- `GET /achievements` (catalog) + `GET /profile/achievements` (earned, M/C/A via the Progress module's existing `ResolvesTargetUser` trait). React: 3 most recent badges on the Dashboard, a full locked/unlocked badge grid on the health Profile page (`/profile/health`).
- `activity_logs` write-through extended into member/coach-facing services per the original Phase 9 plan: `ProgramGenerationService::bootstrap()` → `program.created`; `MealPlanController`/`WorkoutPlanController::update()` → `meal_plan.overridden`/`workout_plan.overridden` (only on an actual manual override, not routine completion-toggle noise); `CoachRecommendationService::approve()`/`reject()` → `recommendation.approved`/`recommendation.rejected`.
- Added the `mail` channel (queued via `ShouldQueue`, so SMTP delivery never blocks the triggering request) to `ProgramReady`, `RecommendationReviewed`, and the new `AchievementEarned` notification — one-time/significant events worth an email on top of the in-app bell, now that real SMTP is configured (Phase 9). `ReminderDue` stays database-only, per its existing per-occurrence-cadence reasoning from Phase 6.
- Bug found via live smoke testing while creating test accounts (not part of this phase's own new code, but real): `Admin\UserController::store()` tried to set `email_verified_at` through a plain `User::create()` array, but that column is deliberately excluded from `$fillable` — so every Admin-created account silently never got marked verified, despite the code's clear intent. Fixed using the same `forceFill()` pattern `OnboardingController` already established for `onboarding_completed_at`, with a regression test. No functional impact today since no route currently gates on the `verified` middleware, but the mismatch between code and intent was real.
- Live HTTP smoke test hit an unrelated infrastructure incident: the `akusehat.web.id` PHP-FPM pool had lost its listening socket during a stale reload from the day before and the site was completely unresponsive. Fixed with `systemctl reload php8.4-fpm` (graceful; the two other sites sharing that FPM instance stayed up throughout) before continuing. Once back up: verified the full loop over real HTTP — logged in as a real member, hit both new achievement endpoints, confirmed the Dashboard/Profile pages render the locked/unlocked badge data correctly, ran `EvaluateAchievementsJob` for real and drained the Redis queue to confirm the `AchievementEarned`/`ProgramReady` notifications completed without error (real SMTP, not the `log` driver), and confirmed real PATCH requests to `/meal-plans/{id}` and `/workout-plans/{id}` write real `activity_logs` rows in MariaDB.
- 316/316 tests passing (22 new for Phase 10).

### Phase 9 — Admin Panel (2026-07-30)
- `activity_logs` migration + `ActivityLog` model + `ActivityLogger` service, pulled forward from Phase 10 (which had originally scoped this table) since Phase 9's audit log viewer needs it now. `ActivityLogger::log(action, ?subject, meta)` is wired into every admin write action built so far: user create/update/delete, role permission sync, coach assign/unassign, AI provider/model create/update, and rule engine rule create/update/deactivate. Member/coach-facing write-through (program creation, plan override, recommendation approval) stays Phase 10 scope as originally planned.
- `users.last_login_at` — a schema column that existed since Phase 1 but was never actually written anywhere — is now recorded via a `RecordLastLogin` listener on Laravel's built-in `Illuminate\Auth\Events\Login` event, needed for the Active Users analytics metric.
- `AnalyticsService` + `/admin/analytics`: Active Users (`status=active` with a login in the last 30 days), Program Completion % (`completed` / total `user_programs`), Avg Health Score (average of each user's *latest* `health_scores.score`, selected portably across MySQL/SQLite via a `MAX(id)`-per-user subquery — ids are monotonic and a given day's score is upserted in place, so this reliably picks the latest row without a MySQL-only window function), and AI Cost 30d + AI Cost by Provider (`ai_request_logs.estimated_cost` summed/grouped over the trailing 30 days). React page matches wireframe/admin.md's "Analytics Landing": 4 stat cards + a provider cost breakdown bar.
- `ActivityLogController` + `/admin/activity-log`: paginated audit log viewer with action/user filters.
- Both new routes gated by the already-seeded `analytics.view` permission (admin-only) rather than a new permission, since they're the same "observability" concern; Admin sidebar nav now lists Users/Roles/Rule Engine/AI Providers/Analytics/Activity Log together as one consolidated Admin section.
- KB CRUD, Admin Prompt Template editor, and a dedicated AI request-log viewer remain deferred (not listed as Phase 9 checklist bullets, despite being depicted/required by the wireframe/PRD) — same phase-scoping precedent followed since Phase 4.
- 294/294 tests passing (16 new for Phase 9). Live HTTP smoke test against the real vhost: a temporary admin account logged in over real HTTP (confirming `last_login_at` actually gets written), loaded both new Inertia pages, triggered a real PATCH (role permission sync) and confirmed the resulting `activity_logs` row landed in the real MariaDB database (not just SQLite), and confirmed a real member session gets a real 403 from both new routes. Temporary account and test log row cleaned up afterward.

### Phase 8 — Coach Module (2026-07-30)
- Migrations for `coach_profiles`, `coach_members`, `coach_notes`, `conversations`, `messages`, `reviews`. `coach_members`' literal `UNIQUE(coach_id, member_id, status)` — flagged "Unique-ish" in the Database Dictionary itself — is dropped: it would break re-assigning a member back to a coach they'd previously left, since that collides with the earlier `(coach_id, member_id, 'ended')` row. "One active assignment per member" is enforced app-side instead, the same precedent as Phase 5's `user_ai_settings` unique-constraint fix.
- `CoachAssignmentService::assign()`/`unassign()`: ends the member's prior active assignment (with whoever it was with) and creates the new one in a transaction, also syncing the member's active `user_programs.coach_id` so Phase 6's program-level authorization stays consistent with this phase's coach-member relationship. A real bug surfaced here during testing: `$member->activeCoachAssignment` as a property access caches the relation on that PHP object instance, so calling `assign()` then `unassign()` on the same `$member` reused a stale cached `null` from before the row existed and silently skipped ending it — fixed by always using the method-call form (`activeCoachAssignment()->first()`), which queries fresh.
- Coach accounts are Admin-created (`Admin\UserController::store()` now auto-creates a blank `coach_profiles` row whenever the new user's role is `coach`); the coach self-service edits their own bio/specialization/certification afterward.
- Coach Dashboard aggregates the coach's active caseload with a "🔴 Perlu Perhatian" section sourced from `ai_memories` (`memory_type=concern`) and pending `ai_recommendations`, per wireframe/coach.md. Member Detail shows profile summary, an AI Advisory panel (new `coach_review` capability call — but only when there's actually a concern or pending recommendation to advise on, to avoid a wasted AI call on every page view for members who are doing fine), pending-recommendation approve/reject, and private coach notes (default not visible to the member, per-note visibility toggle).
- Chat: `conversations`/`messages` support both `coach_member` (plain messaging) and `ai_assistant` (Phase 5's `daily_chat` capability, called synchronously rather than via a queued job — chat is an actively-watched conversation, unlike program generation's background latency). Real-time delivery uses the documented polling fallback (4s re-fetch) since no Echo/Pusher/Reverb credentials exist in this environment.
- Reviews: one per `(coach, member)` pair, upserted (a second submission updates rather than duplicates) with `coach_profiles.rating_avg` recalculated on every new review. Added to the Program detail page whenever that program has an assigned coach.
- Permissions: reused Phase 1's already-seeded `member.view`/`program.review`/`note.manage`/`chat.send` grants (anticipated back in Phase 1, unused until this phase) instead of inventing a parallel role-check middleware; added one new `coach_members.manage` permission for the Admin assignment endpoint.
- Extended Phase 7's Progress page to accept `?user_id=` for the Coach's read-only view — wireframe/progress.md called for this back in Phase 7 ("Coach viewing this same page... scoped to the selected member") but it was unreachable until this phase's coach-assignment mechanism existed; non-shared private photos are filtered out for non-owners.
- 278/278 tests passing (40 new for Phase 8). Live HTTP smoke testing exercised the full loop for the first time with two real accounts and a live vhost: Admin creates a coach account and assigns a member to them, the coach views their dashboard/member detail, adds a private note, exchanges coach_member chat messages, approves a pending AI recommendation (member gets notified), and the member submits a coach review (rating average recalculates) — plus a fresh third account confirmed every one of those endpoints correctly returns 403 without the right relationship.

### Phase 7 — Progress Tracking & Health Score (2026-07-30)
- Migrations for `weight_logs`, `waist_logs`, `body_fat_logs`, `sleep_logs` (all unique-per-user-per-day), `water_intake_logs`/`progress_photos` (multiple per day), and `health_scores`. `weight_logs`/`waist_logs` now supersede `body_measurements` (Phase 3) as the primary weight/waist source, fulfilling the forward-looking comment left in Phase 3's `HealthProfileService`.
- `HealthScoreService`: the documented 8-component weighted formula (BMI, waist, sleep, water, activity, weight trend, checklist consistency, disease-management adherence) from docs/08-Knowledge-Base.md §5. The doc names each component's weight and "basis" but not an exact scoring curve, so each component's decay function is designed and documented inline — e.g. BMI loses 2 points per unit outside the 18.5-24.9 healthy range; waist uses IDF Asian-population cutoffs (male <90cm/female <80cm) relevant to this app's Indonesian userbase; disease-management adherence is proxied via `meal_plans.is_completed` since there's no per-meal "what did you actually eat" logging in this app, defaulting to full marks when a user has no active disease restrictions to manage. 17 unit tests pin every component against known, hand-calculated inputs.
- `ComputeHealthScoreJob` (daily, all onboarded users) computes the score then asks the `analyze()` AI capability (new `health_score_explain` prompt template) to narrate it in plain language — explicitly instructed to explain, never recompute, the deterministic score. Falls back to a deterministic "your weakest component is X" explanation when no AI provider is available, matching every other AI-touching job in this app.
- Full CRUD API for weight/waist/body-fat/sleep/water/photos/health-score (§7 of 05-API-Specification.md), with M/C/A access via a `?user_id=` query param checked against `user_programs.coach_id` — the same pattern the Programs module already used, ahead of Phase 8's proper coach-assignment UI.
- Progress photos: stored on the `local` disk (`storage/app/private`, never web-accessible) regardless of privacy setting, served only through a 30-minute temporary signed URL. `is_private` toggling doubles as the wireframe's "Bagikan ke Coach" mechanism — no separate sharing column needed.
- Member Progress page (`/progress`) per wireframe/progress.md: Health Score trend (tap a point to read that day's explanation), weight/waist mini-charts with quick-log dialogs, sleep/water 7-day averages, a photo timeline with upload/share/delete, and a 14-day checklist consistency grid. The Minggu/Bulan/90-Hari range selector filters an already-loaded 90-day dataset client-side instead of re-querying per range — added `recharts` for the charts (no shadcn chart wrapper existed yet). Health Score card also added to the Dashboard, deferred from Phase 6 pending this phase's tables.
- Two bugs fixed, both pre-existing (not introduced this phase) and both found via live HTTP smoke testing: (1) the same weight-source fallback chain (`weight_logs` → `body_measurements` → `health_profiles.initial_weight_kg`) was independently duplicated across `HealthProfileService`, `RuleEngineService`, `PromptBuilderService`, `AIMemoryService`, and `DashboardController` — consolidated into `User::latestWeightKg()`/`latestWaistCm()` rather than adding a fifth copy while wiring in the new tables. (2) Manually rebuilding a response array from a date-cast Eloquent attribute (`['start_date' => $model->start_date]`) silently bypasses the `'date:Y-m-d'` cast format, since that format is only honored by the model's own `toArray()`/`toJson()` — Carbon's default JSON serialization (a full UTC-shifted ISO8601 timestamp) takes over instead. Found on `ProgressPhotoController`, `ProgressPageController`, and `DashboardController`; fixed with explicit `->toDateString()` calls, each with a regression test. Notably this is pure PHP/Carbon behavior, not a MySQL-vs-SQLite difference, so once the missing assertions were written the bug reproduced identically under the PHPUnit suite's SQLite connection — it simply hadn't been asserted for before.
- 238/238 tests passing (51 new for Phase 7, including 3 regression tests for the date-serialization bug).

### Phase 6 — Program Generation (2026-07-30)
- Migrations for `programs`, `user_programs`, `program_goals`, `weekly_plans`, `daily_tasks`, `meal_plans`/`meal_plan_items`, `workout_plans`/`workout_plan_items`, `checklist_items`, `reminders`, plus the stock `notifications` table and a follow-up migration finally adding the `ai_memories`/`ai_recommendations.user_program_id` foreign keys Phase 5 deferred until this table existed. `weekly_plans` also gets a `viewed_at` column (not in `mysql.sql`) to support the dashboard's "Weekly Review card disappears once viewed" behavior.
- `ProgramGenerationService`: Goal → Rule Engine → `AIGatewayService` (`generatePlan` capability, reusing Phase 5's `meal_plan`/`workout_plan` templates) → persist. Generates **one day at a time** rather than the full 90 days upfront — a plan that's meant to adapt weekly has no reason to pre-generate months of content that will likely be superseded, and doing so would mean 180+ AI calls just to start a program. Falls back to a deterministic Rule-Engine-only plan (picked directly from `kb_foods`/`kb_exercises`) whenever no AI provider is configured or all configured providers fail.
- `GenerateProgramJob` (queued, dispatched from onboarding completion and the `/regenerate` endpoint) + a cache-backed generation-status endpoint (no schema column reserved for this ephemeral state).
- `AIMemoryService`: deterministic milestone/concern/pattern/trend detection from checklist completion and weight history (7 fully-completed days → milestone, 3 zero-completion days → concern, low workout adherence vs. high meal adherence → pattern, ≥0.5kg 14-day weight change → trend). `ScanAIMemoryJob` (daily) and `PruneAIMemoryRelevanceJob` (weekly relevance decay) scheduled; also invoked incrementally on every checklist/task check-in.
- `RecommendationApplierService`: only `habit`/`motivation` weekly-review adjustments ever auto-apply, even when the AI marks a `meal_adjustment`/`workout_adjustment` as `auto_applicable` — the AI's `detail` field is free text, not a structured delta, so there's no sound way to bounds-check a mutation to `meal_plan_items`/`workout_plan_items` against it. Structural adjustments always go to Coach approval instead of trusting a fabricated text-parsing heuristic.
- `GenerateWeeklyReviewJob`, dispatched by a daily scheduler tick that finds whichever programs' current week just ended (a program's week boundary is relative to its own `start_date`, not a shared calendar day).
- `DispatchRemindersJob` (every minute, timezone-aware via `users.timezone`, `last_sent_at` as the once-per-day dedup guard) delivering reminders as in-app database notifications (the dashboard's 🔔 bell). Onboarding's 4 reminder-time answers (water/meal/workout/check-in) now actually create `reminders` rows, and its target-weight/timeframe/notes answers now create the program's `program_goals` row — both previously-collected-but-unused pieces of onboarding data.
- Member Dashboard (today view) per wireframe/dashboard.md: program progress card, latest weight, today's checklist (togglable), new-weekly-review card, multi-program switcher. Health Score card is deferred to Phase 7 (`health_scores` doesn't exist yet) rather than faked. Program detail and weekly-review detail pages added.
- Events `ProgramGenerated` → in-app "program ready" notification, `CheckInSubmitted` → incremental AI Memory scan, `AIRecommendationCreated` → coach notification (checks `user_programs.coach_id`, which exists now even though Phase 8's coach-assignment UI doesn't yet — safely a no-op until then).
- Two bugs found by live HTTP smoke testing — pre-existing in Phase 3's onboarding code, not introduced this phase, but only reachable once a real program-generation flow exercised onboarding completion end-to-end against real MySQL: (1) `HealthProfileService`'s BMI/BMR/TDEE math has no input sanity bounds, so an unrealistic height (e.g. a typo) produces a BMI that overflows `health_profiles.bmi`'s `DECIMAL(5,2)` under MySQL's strict mode — invisible against the test suite's SQLite, which doesn't enforce DECIMAL range at all. (2) A "repeatable" onboarding question (medications/allergies) had no server-side check that its answer was actually an array, so a plain-string answer crashed `MapOnboardingAnswersToHealthProfile::mapRepeatable()` with a `TypeError`. Both fixed with boundary validation in `OnboardingController::answer()` (min/max bounds sourced from `onboarding_questions.validation_rules` for the 5 body-measurement questions, an `array` rule for repeatable questions), each with a regression test.
- 184/184 tests passing (32 new for Phase 6, 2 new onboarding regression tests for the bugs above).

### Phase 5 — AI Provider Layer (2026-07-29)
- Migrations/models for `ai_providers`, `ai_models`, `user_ai_settings`, `ai_prompt_templates`, `ai_memories`, `ai_recommendations`, `ai_request_logs`. `user_ai_settings` intentionally drops `mysql.sql`'s literal `UNIQUE(user_id, is_default)` index — MySQL has no partial/filtered unique index, so a plain 2-column unique also caps *non*-default rows at one per user, which breaks FR-AI-06's requirement for a second (failover) provider per user. "One default per user" is enforced app-side instead: every write path that sets `is_default = true` unsets the others in the same transaction.
- `AIProviderInterface` (7 methods: `analyze`, `chat`, `generatePlan`, `weeklyReview`, `dailyMotivation`, `mealSuggestion`, `workoutSuggestion`) with 6 adapters — `OpenAIProvider`, `ClaudeProvider`, `GroqProvider`, `GeminiProvider`, `OllamaProvider`, `LMStudioProvider` — sharing a common `AbstractHttpProvider` base (JSON decoding with brace-extraction fallback, transport-error → `AIProviderException` mapping). `AIProviderFactory` resolves each provider's `driver_class` column to a concrete instance at runtime.
- `PromptBuilderService`: resolves `{{variable}}` placeholders per template (user profile, Rule Engine output, KB context, AI memory context, progress snapshot, etc.) against the seeded `prompts/*.txt` templates; the fixed JSON-output instruction block lives in the template text itself rather than being appended separately.
- `AIResponseProcessor`: up to 2 corrective retries on invalid JSON or failed schema validation (via a custom lightweight `JsonSchemaValidator`), falling back to Rule-Engine-only output plus an audit `ai_recommendations` row if all attempts are exhausted.
- `AIGatewayService`: resolves the user's default provider, times and logs every call to `ai_request_logs`, and fails over to a secondary configured provider on transport error — both the primary's failure *and* the secondary's outcome are logged (a gap caught during manual verification: failover succeeding was silently swallowing the primary's failed attempt).
- Member Settings → "AI Provider" (`/ai/settings`): CRUD for provider/model/API key/temperature (API key stored `Crypt::encryptString()`'d, never exposed back to the page), "Test Connection" (a direct provider call bypassing the retry/validation pipeline), "Jadikan Default". Shown as a list of configured providers rather than the wireframe's single radio group, since failover requires more than one saved provider at once.
- Admin → "AI Providers" (`/admin/ai/providers`): provider active/inactive toggle, per-provider model CRUD.
- Two bugs caught by live HTTP smoke testing (not the in-process test suite): (1) `GeminiProvider` chained `->withQueryParameters(['key' => ...])` after `->post()` instead of before, silently making it a no-op on the response object rather than the request. (2) `AiSettingsController::validated()` read `$validated['api_key']` without a null-coalesce — fine when the form always sends the field (as every PHPUnit test did), but a real request that omits `api_key` entirely (valid for local providers like Ollama that need no key) threw an "Undefined array key" 500. Both fixed; the second got a regression test.
- 150/150 tests passing (added 1 regression test for the missing-`api_key` bug). Deferred to a later phase: Admin Prompt Template editor UI, Admin AI request-log viewer/cost dashboard, and a real-provider integration test (no API keys/local model server available in this environment — covered instead by `Http::fake()` tests plus a live smoke test against the real OpenAI API with an invalid key and a local Ollama endpoint with nothing listening, both of which degraded gracefully).

### Phase 4 — Knowledge Base & Rule Engine (2026-07-29)
- Migrations/models for `kb_foods`, `kb_exercises`, `kb_nutrition_articles`, `kb_faqs`, `rule_engine_rules` (`kb_diseases` was already done in Phase 3).
- Starter/demo KB content — 40 foods, 28 exercises, 3 articles, 3 FAQs — explicitly **not** the full ~300–500-row SME/TKPI-sourced catalog docs/08-Knowledge-Base.md §7 calls for; per PRD §6.3, full content curation is a product-owner/SME task, not something to fabricate wholesale. Every seeded food's `source` field flags it as pending review.
- `RuleEngineConditionEvaluator`: a constrained DSL interpreter (`>=`,`<=`,`>`,`<`,`==`,`in`,`and`,`or`,`not`), never `eval()`'d, per 04-Architecture.md §8. A missing/null context field never matches (fails closed, not open).
- `RuleEngineService::evaluate(User): array` — the fixed 5-key output contract from 08-Knowledge-Base.md §3.3. Within a category, same-key conflicts resolve by priority (higher wins); `disease_restriction` is the one category that **unions** instead — two conditions mean two sets of restrictions apply, not just the higher-priority one's.
- Seeded 11 baseline rules across all 5 categories, including the doc's 3 worked examples verbatim. Full pipeline verified against realistic profiles both in unit tests and live: a 33.2-BMI diabetic sedentary user correctly gets a 25%-deficit calorie target (obese threshold overriding the plain overweight rule), a diabetes-adjusted macro split, `beginner` workout level, and a `low_glycemic_index` restriction — matching manual calculation exactly.
- Read-only KB search endpoints (`GET /kb/foods|exercises|diseases|articles|faqs`, `?q=&category=&tags[]=`). Admin CRUD for editing this content is deferred (only Rule Engine has an Admin UI so far).
- Admin Rule Engine CRUD + "Uji Coba" — dry-runs a rule's condition against a hand-entered sample profile without touching a real user, showing whether it matches and what action would apply.
- 110/110 tests passing, including a full worked-example integration test matching 08-Knowledge-Base.md §3.3's documented output shape exactly.

### Phase 3 — Health Profile (2026-07-29)
- Migrations/models for `health_profiles`, `lifestyle_profiles`, `user_diseases`, `user_allergies`, `user_medications`, `body_measurements`, plus `kb_diseases` pulled forward from Phase 4 (only that one KB table + a 5-row seed, since `user_diseases.kb_disease_id` is a required FK — kb_foods/kb_exercises/kb_nutrition_articles/kb_faqs and their Admin CRUD stay in Phase 4).
- `HealthProfileService`: BMI/BMR (Mifflin-St Jeor)/TDEE, verified against the worked example in docs/07-Prompt-Engineering.md §4 (age 39/167cm/77.5kg → BMI 27.79, BMR 1628.75, TDEE 2239.53 at "light" activity).
- Weight source precedence: most recent `body_measurements` row over `health_profiles.initial_weight_kg` (the onboarding-time baseline) — `weight_logs` (Phase 7) will become the primary trigger once it exists.
- `BodyMeasurementObserver`/`LifestyleProfileObserver` recalculate BMI/BMR/TDEE automatically on a new measurement or an `activity_level` change.
- `MapOnboardingAnswersToHealthProfile` listener populates all six tables from the 55 onboarding answers, matched by `onboarding_questions.step` (coupled to `OnboardingQuestionSeeder` — documented inline since the mapping breaks silently if the two drift apart).
- Settings → "Kesehatan" page (`/profile/health`): editable health/lifestyle fields, disease/allergy/medication add-remove, and a measurement log — all Member-own-data only; Coach/Admin read access is deferred to Phase 8 (`coach_members` doesn't exist yet).
- Two real bugs caught by live HTTP smoke testing (not the in-process test suite): (1) Laravel 12's `Application::configure()` auto-discovers `app/Listeners` by convention, which was silently double-registering every explicitly-`Event::listen()`'d listener (2 listeners → 4 registered) and caused a duplicate-insert crash under concurrent-looking execution — fixed with `->withEvents(discover: false)` in `bootstrap/app.php` so registration order in `AppServiceProvider` is the only source of truth. (2) `date`-cast fields (`date_of_birth`, `measured_at`, etc.) serialize to JSON as full UTC-shifted ISO8601 timestamps by default, which silently breaks `<input type="date">` and can shift the date by a day relative to `APP_TIMEZONE` — fixed with the `date:Y-m-d` cast format across every date-only column.
- 68/68 tests passing (added BMI/BMR/TDEE unit tests, onboarding-mapping feature tests, and profile API CRUD feature tests).

### Phase 2 — Onboarding (2026-07-29)
- Migrations + models for `onboarding_questions`/`onboarding_sessions`/`onboarding_answers`, matching the Database Dictionary (note: `onboarding_answers` has no `created_at`/`updated_at`, only `answered_at` — `OnboardingAnswer::$timestamps = false`).
- 55 seeded questions across identity/body/lifestyle/medical/preferences/goal (wireframe/onboarding.md only sketches step ranges, not literal text — authored here). Medications/allergies use a `validation_rules.repeatable` flag consumed by a generic repeatable-row React component, rather than a one-off special case.
- Wizard API (`/onboarding/*`): resumable session start (idempotent — returns the existing `in_progress` session if one exists), per-question answer autosave advancing `current_step`, and a `complete` endpoint that 422s with the specific missing questions if any required question is unanswered.
- React wizard shell (one question per screen, progress bar, back/skip/next) with input components per `input_type`, driven by plain `fetch` (`resources/js/lib/api.ts`) rather than Inertia visits, so answering a question doesn't trigger a full page reload.
- `OnboardingCompleted` event → `DispatchInitialProgramGeneration` listener → `GenerateInitialProgram` job. The job is a **logging stub**: the real Goal → RuleEngine → AI pipeline is Phases 4-6, not built yet, so this intentionally doesn't fake program generation ahead of the layer it depends on.
- Registration now redirects members to `/onboarding` instead of the dashboard; a new `onboarding.completed` middleware gates the dashboard (and future member-only routes) behind a completed wizard, scoped to the `member` role only.
- Fixed a mass-assignment bug caught by the test suite before it ever reached production: `User::update(['onboarding_completed_at' => ...])` was silently dropped because that column isn't (and shouldn't be) in `$fillable` — it's system-managed, not user-editable. Fixed with `forceFill()` in the one legitimate place that sets it.
- Full 55-question flow verified end-to-end over real HTTP (not just the in-process test suite): register → redirect to wizard → answer all questions → complete → session marked `completed`, `users.onboarding_completed_at` set, job visibly queued in Redis and processed cleanly.
- Not done: Admin question-bank CRUD UI (create/edit/reorder/deactivate) — deferred alongside the rest of the Admin panel.
- 49/49 tests passing.

### Phase 1 — Core / Auth / RBAC (2026-07-29)
- Real `roles`/`permissions`/`role_permissions`/`users` migrations matching the Database Dictionary, replacing the starter kit's placeholder schema.
- `Role`/`Permission` Eloquent models, `User::hasPermission()`/`hasRole()`, `EnsurePermission` middleware (`permission:<name>` route alias).
- `RolePermissionSeeder`: baseline admin/coach/member roles and the permission set from PRD §10.
- Registration assigns the `member` role by default; `GET /api/v1/auth/me` (Sanctum-protected) returns the user with role + permissions.
- Admin panel: Users CRUD (search/filter by role & status, create/edit/soft-delete) and Roles & Permissions management (per-role permission matrix), both gated behind `permission:users.manage` / `permission:roles.manage`. Admin nav section only renders for users with `users.manage`.
- Fixed an infra bug found via live smoke testing (not caught by the test suite, which runs in-process): server-wide ModSecurity (OWASP CRS) was blocking all PATCH/DELETE requests with a 403 before they reached Laravel. Fixed with a `tx.allowed_methods` override scoped to the `akusehat.web.id` vhost only (see the comment in `/etc/apache2/sites-available/akusehat.web.id.conf`, outside this repo).
- 41/41 tests passing (Pest/PHPUnit, in-memory SQLite).

### Phase 0 — Project Setup (2026-07-29)
- Scaffolded Laravel 12 via `laravel/react-starter-kit` (Inertia + React + TypeScript + Tailwind + ShadCN), added Sanctum and Horizon.
- Wired to the real MySQL/MariaDB database and Redis; serving live at `https://akusehat.web.id`.
- Base `app/Services`, `app/Repositories`, `app/Contracts` folders per [04-Architecture.md](04-Architecture.md) §2.
- Not done: CI pipeline (workflow files exist but aren't pushed — token scope gap) and error tracking (no Sentry DSN yet).

## [1.0.0] - 2026-07-29

### Added
- Initial full documentation set generated from the product discussion in [chat_dengan_chatgpt.txt](../chat_dengan_chatgpt.txt):
  - `docs/01-PRD.md` — Product Requirements Document
  - `docs/02-ERD.md` — Entity Relationship Diagrams (11 module diagrams, 57 tables)
  - `docs/03-Database-Dictionary.md` — field-level reference for every table
  - `docs/04-Architecture.md` — System architecture, AI abstraction, jobs, security
  - `docs/05-API-Specification.md` — REST API specification (100+ endpoints)
  - `docs/06-AI-Provider-Interface.md` — `AIProviderInterface` contract and adapter pattern
  - `docs/07-Prompt-Engineering.md` — Prompt Builder mechanics and template catalog
  - `docs/08-Knowledge-Base.md` — Knowledge Base content model and Rule Engine DSL
  - `docs/09-UI-UX-Wireframe.md` + `wireframe/*.md` — design system and page wireframes
  - `docs/10-Roadmap.md` — v1.0 → v3.0 roadmap
  - `docs/11-Development-Checklist.md` — phase-by-phase build task breakdown
  - `database-schema/mysql.sql` — canonical MySQL 8 schema (source of truth)
  - `database-schema/erd.dbml` — DBML mirror for dbdiagram.io visualization
  - `prompts/*.txt` — seed AI prompt templates (onboarding, meal-plan, workout, weekly-review, daily-chat, coach-review)

### Decisions Locked In (from product discussion)
- Platform positioning: **AI Personal Health Coach**, not a diet-only app; "Diet & Transformasi 90 Hari" is module 1 of an extensible platform.
- Architecture: Rule Engine + Knowledge Base compute deterministic ground truth; AI reasons over it and returns JSON only, validated against a schema before persistence or display.
- Stack: Laravel 12, PHP 8.4, Inertia + React, TailwindCSS + ShadCN, MySQL, Queue + Scheduler.
- Roles: Admin, Coach, Member.
- AI may auto-adjust programs within Rule-Engine-defined bounds; out-of-bounds changes require Coach approval.
- Users may run multiple programs concurrently/sequentially.
- Multi-provider AI support (OpenAI, Claude, Groq, Gemini, Ollama, LM Studio) via a shared `AIProviderInterface`, user-configurable per account.

### Open Questions Carried Forward
See [docs/01-PRD.md](../docs/01-PRD.md) §12 — AI cost/billing model, coach assignment mechanism, medical liability posture, data residency.
